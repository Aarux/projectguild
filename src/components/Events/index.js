import React from "react";
import { Events } from "../../styles/GlobalStyle";

const EventsPage = () => (
  <Events>
    <h1>Events</h1>
    <p>Sida där Officers/Raiders kan lägga upp raid events{"(?)"}</p>
  </Events>
);

export default EventsPage;
